<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <rect width="512" height="512" rx="112" fill="#10b981"/>
  <!-- Pio spark star, centered and large -->
  <g transform="translate(256,256)">
    <!-- Outer glow circle -->
    <circle r="110" fill="#34d399" opacity="0.35"/>
    <!-- Inner body circle -->
    <circle r="88" fill="#a7f3d0" opacity="0.3"/>
    <!-- Star spark shape -->
    <path d="M 0 -90 L 18 -22 L 90 0 L 18 22 L 0 90 L -18 22 L -90 0 L -18 -22 Z"
          fill="white" opacity="0.95"/>
    <!-- Small center dot -->
    <circle r="18" fill="white"/>
  </g>
</svg>
